apiVersion: v1
kind: ServiceAccount
metadata:
  name: default-restricted-sa
  namespace: default
---
apiVersion: v1
kind: Secret
type: kubernetes.io/service-account-token
metadata:
  name: default-restricted-secret
  namespace: default
  annotations:
    kubernetes.io/service-account.name: "default-restricted-sa"
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: default-restricted-role
  namespace: default
rules:
  - apiGroups: ["*"]
    resources:
      [
        "pods",
        "services",
        "configmaps",
        "secrets",
        "pods/log",
        "deployments",
        "daemonsets",
        "statefulset",
        "replicasets",
        "serviceaccounts",
        "ingresses",
        "events",
      ]
    verbs: ["get", "list", "watch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: default-restricted-role-binding
  namespace: default
subjects:
  - kind: ServiceAccount
    name: default-restricted-sa
    namespace: default
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: default-restricted-role
