apiVersion: v1
kind: ServiceAccount
metadata:
  name: cluster-restricted-sa
  namespace: kubernetes-dashboard
---
apiVersion: v1
kind: Secret
type: kubernetes.io/service-account-token
metadata:
  name: cluster-restricted-secret
  namespace: kubernetes-dashboard
  annotations:
    kubernetes.io/service-account.name: "cluster-restricted-sa"
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: cluster-restricted-role
rules:
  - apiGroups: ["*"]
    resources:
      [
        "nodes",
        "namespaces",
        "pods",
        "services",
        "configmaps",
        "secrets",
        "pods/log",
        "deployments",
        "daemonsets",
        "statefulset",
        "replicasets",
        "serviceaccounts",
        "ingresses",
        "events",
      ]
    verbs: ["get", "list", "watch", "update"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: cluster-restricted-role-binding
subjects:
  - kind: ServiceAccount
    name: cluster-restricted-sa
    namespace: kubernetes-dashboard
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: cluster-restricted-role
